#include <stdio.h> 
int main(void) 
{ 
 printf("Parkinson's Law:\nWork expands so as to "); 
 printf("fill the time\n"); 
 printf("available for its completion.\n"); 
 return 0; 
}
// 程序的指令就是以#开头的包含指令，用于指示预处理器在预处理阶段将要包含的头文件包含到程序的前面来。
// 语句来说我认为有两种理解方式，其一就是int main(void)和{}包含起来的整体语句。
// 或者就是按照分号来结尾的行。
// 程序的输出有三行字符串。
